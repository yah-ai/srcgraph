# srcgraph (architecture)

Canonical design doc lives at the workspace root: [../../../DESIGN.md](../../../DESIGN.md).

This thin pointer exists so the doc shows up under the canon location
(`.yah/docs/architecture/`) per yah's docs-layout convention, while the actual content
stays at repo root for crates.io / GitHub discoverability.
